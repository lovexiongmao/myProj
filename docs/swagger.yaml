info:
  contact: {}
paths:
  /ping:
    get:
      consumes:
      - application/json
      description: 测试的接口
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - myTest
swagger: "2.0"
